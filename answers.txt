

1. 
timing:
reddit-0                        :        real 0m30.943s
reddit-2b, no schema, no caching:        real 0m56.621s
reddit-2b, schema, no caching   :        real 0m40.525s
reddit-2b, schema + caching     :        real 0m42.249s



2. 
most of the time is spent  in reading.
i am decompressing and JSON-parsing the input file. So, not calculating the averages. 
from 1.
reddit-2b, no schema, no caching:        real 0m56.621s
reddit-2b, schema, no caching   :        real 0m40.525s

so providing the schema avoids this extra work.

for averages calculation is only a groupBy and average.
so it take less time than reading and parsing the JSON. 
schema + caching took 42.249s, which was bit slower than the
schema + no caching. i think small it caused by timing variation and the cost of placing the DataFrame into the
cache. 



3. 
I used .cache() once on the cleaned pagecounts DataFrame. 
so basically after extracting the hour from the filename, 
filtering for english wikipedia pages

This DataFrame is used twice.
to calculate the maximum number ofviews for each hour 
and then again when joining the max values back to
the pagecount rows.